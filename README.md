# k-mean
Implementation of K-Means Clustering using scikit-learn to form optimal clusters, visualize results, and evaluate model performance using the elbow method.
K-Means Clustering – Lab 9

This experiment demonstrates the K-Means Clustering algorithm, an unsupervised machine learning method used to group data points into clusters based on similarity.

🎯 Objective

To perform K-Means Clustering on a given dataset, determine the optimal number of clusters using the elbow method, and visualize the clustered output.

🧠 Key Steps

Load and preprocess dataset (e.g., Iris dataset)

Select relevant features (Petal Length and Width)

Apply K-Means algorithm from scikit-learn

Use the elbow method to find the best k value

Visualize clusters and centroids

Evaluate clustering results

📊 Outputs

Elbow plot to determine optimal k

Cluster visualization graph

Final cluster centroids

💻 Tools Used

Python

NumPy

Pandas

Matplotlib

Scikit-learn
